[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
norecursedirs = oduit integration_tests
addopts = -p no:odoo -m "not integration"
markers =
    integration: marks tests as integration tests (run against real Odoo, deselected by default)
