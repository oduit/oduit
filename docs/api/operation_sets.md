# Operation Sets

```{eval-rst}
.. automodule:: oduit.operation_sets
   :members:
   :undoc-members:
   :show-inheritance:
```

## Overview

`oduit.operation_sets` parses, validates, resolves, inspects, writes, and
aggregates reusable operation set files. It is used by the CLI `set` command
group and by commands that save addon lists with `--save-set`.
